from clld.web.assets import environment
from path import path

import {{package}}

_dir = path({{package}}.__file__).dirname().joinpath('static')
_url = '/{{package}}:static/'

environment.directory = _dir
environment.url = _url
environment.append_path(_dir, url=_url)
environment.load_path = reversed(environment.load_path)
